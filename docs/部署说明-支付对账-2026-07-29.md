# 操作手册 · 支付掉单修复与对账上线

> 最后更新 2026-07-30 · 经三轮交叉审查（Fable 5 两轮 + Opus 5 一轮）
>
> 代码提交：`3bfafce` `6f8af61` `49571a7` `6a5dfe4` `4b51b97` `4781bcb` `26fe71e` `01c7ce9` `6e378f7`
>
> **这是一份可以从头执行到尾的操作手册，按顺序做即可，不需要跳到别的文档。**
>
> ⚠️ **本手册在执行过程中修订过两次**（详见文末「附录 · 修订记录」）。
> 若你已执行到步骤 5，请从「⚠️ 步骤 6 原方案已作废」那一节继续，不要按旧的步骤 6、7 操作。

---

# 一、需要先了解的情况

## 1.1 出了什么事

2026-07-28 15:21，一位游客购买「水上城堡-双人票」¥49.90，微信扣款成功，
但小程序订单一直显示「待支付」，核销码没有生成。

## 1.2 根因（已由数据完整还原，非推测）

```
15:20:33      创建订单 PROD20260728072033017383
15:20:58      用户绑定手机号成功 → 系统启动一个 1.5 秒后自动继续支付的定时器
15:21:00.5    定时器触发 → 自动发起支付流程
              ⚠️ 这期间页面没有任何加载提示，按钮还能点
15:21:02.656  → 向微信申请到商户单号 A，写入数据库
15:21:04.252  用户见页面毫无反应，点了「立即支付」→ 申请到单号 B，覆盖了 A
15:21:09      用户在第一个支付窗口完成付款，微信按单号 A 扣款
              ↓
              微信的「支付成功」通知带着 A 回来
              数据库里存的却是 B → 查不到订单 → 静默失败
              订单永久停在「待支付」
```

**三个缺陷叠加**：① 自动继续支付时无提示，诱导用户重复点击 ② 没有防重复提交锁
③ 每次申请都覆盖旧单号，而不是复用。

**责任完全在系统，与用户操作无关。**

## 1.3 本次做了什么

| 层面 | 内容 | 作用 |
|---|---|---|
| **根治** | 支付按钮加锁、加载提示、复用商户单号 | 同类问题不再发生（需小程序发版） |
| **兜底** | 每 10 分钟主动向微信核对一次支付状态，异常自动修复 | 任何原因掉单都能自愈 |
| **留痕** | 支付异常写入 `pay_exceptions` 集合 | 以后出问题可追溯 |

## 1.4 执行前必须知道的四件事

1. **云函数上传即生效，不需要审核；小程序端改动需要提交审核（1~3 天）**。
   所以先上云函数建立兜底网，小程序随后发版做根治。

2. **游客已经等了两天，不要为了验证再让人干等。** 修复事故订单在步骤 8，
   其中「方式二」一条语句两分钟搞定，随时可以先做。

3. **任何一步结果和预期不符，停下来问我，不要继续往下走。** 这是生产售票系统。

4. **云开发控制台的「云端测试」不能用来验证对账功能**。对账要调微信支付查单接口，
   属于「云调用」，需要微信侧注入的身份令牌；控制台测试和 CLI 都拿不到该令牌，
   必然报 `invalid wx openapi access_token`。**只有小程序端调用和微信定时触发器有令牌。**
   详见步骤 6 前的说明。

---

# 二、操作步骤

> 全程约 40 分钟。每步都有「预期结果」，对不上就停。

## 步骤 1 · 创建 `pay_exceptions` 集合

云开发控制台 → 数据库 → 左侧集合名称旁的 `+` → 输入 `pay_exceptions` → 确定

- [x] 完成

**为什么**：云开发写入不存在的集合会报错 `-502005`。代码里做了容错不会影响主流程，
但所有异常留痕会静默丢失，本次改动的「可追溯」目的就落空了。

> 你们的 `admin_logs` 当初就踩过这个坑。

---

## 步骤 2 · 临时关掉定时器

用编辑器打开 `cloudfunctions/reconcilePayment/config.json`，把整个文件内容改成：

```json
{}
```

- [x] 完成

**为什么**：先手动验证一遍再放开自动运行。验证通过后步骤 9 会恢复。

---

## 步骤 3 · 部署 `reconcilePayment`

微信开发者工具 → 右键 `cloudfunctions/reconcilePayment` 目录
→ **上传并部署：云端安装依赖**

- [x] 部署成功，云函数列表里能看到 `reconcilePayment`

---

## 步骤 4 · 把它的超时改成 60 秒

云开发控制台 → 云函数 → `reconcilePayment` → 配置 → 超时时间 → 改为 **60** → 保存

- [x] 完成

**为什么必须现在改**：云函数默认超时只有 **3 秒**，而对账要向微信查询多次，3 秒必然被杀。
这个配置**只能在控制台改**，代码里的 `config.json` 不支持超时字段。

**为什么不能更早改**：函数没部署之前，控制台里根本没有这一项。

> ⚠️ **待验证**：重新上传代码会不会重置这个配置，我没有实测过。
> 保险起见，**以后每次重新部署 `reconcilePayment`，都回来确认一次超时还是 60 秒**。

---

## 步骤 5 · 用「高级操作」给订单补上真实商户单号

云开发控制台 → 数据库 → 左下角 **高级操作** → 旁边的 `+` → **collection.update 模板**（或空白模板）

粘贴并执行：

```js
db.collection('orders')
  .where({
    orderNo: 'PROD20260728072033017383'
  })
  .update({
    data: {
      'payment.outTradeNoHistory': ['PROD178522326265673']
    }
  })
```

- [x] 执行返回 `updated: 1`

**然后必须验证字段落对了位置**，再建一个模板执行：

```js
db.collection('orders')
  .where({
    orderNo: 'PROD20260728072033017383'
  })
  .get()
```

展开返回的 `payment`，确认 `outTradeNoHistory` **嵌在 `payment` 对象内部**：

```json
"payment": {
  "method": "wechat",
  "outTradeNo": "PROD1785223264252216",
  "outTradeNoHistory": ["PROD178522326265673"],
  "paidAt": null,
  ...
}
```

- [x] 确认字段在 `payment` 里面，不是和 `orderNo`、`status` 同级

**这是在做什么**：`PROD178522326265673` 是微信账单上**实际扣款**的商户单号。
数据库里现在存的 `PROD1785223264252216` 是被覆盖后的那个，从没被支付过。
补上真实单号，下一步对账函数才能拿它去问微信。

> **不要用「添加字段」弹窗做这件事**。那个弹窗用点号命名时，可能创建一个字面名叫
> `payment.outTradeNoHistory` 的**顶层字段**而不是嵌进 `payment`，无法确认。
> 而 update 语句里的点号是标准的嵌套路径语义，没有歧义。
>
> 另外：云开发数据库是文档型（MongoDB 兼容），**语法不是 SQL**，
> 但「高级操作」就是语句执行入口，能力完全够用。

---

## ⚠️ 步骤 6 原方案已作废 —— 请从这里继续

**你在原步骤 6 遇到的报错是我的设计错误，不是你操作错误。**

原步骤 6、7 都让你用云开发控制台的「**云端测试**」来跑对账，但对账要调用微信支付查单接口
（属于「云调用」），而**云调用需要微信侧注入的身份令牌，控制台测试根本拿不到**。

依据 —— `@cloudbase/node-sdk/lib/utils/wxCloudToken.js` 源码里的原文注释：

```
由定时触发器触发时（TRIGGER_SRC=timer）：优先使用 WX_TRIGGER_API_TOKEN_V0，
不存在的话，为了兼容旧的开发者工具，也是使用 WX_API_TOKEN
非定时触发器触发时（TRIGGER_SRC!=timer）: 使用 WX_API_TOKEN
```

| 调用来源 | 令牌 | 能否跑对账 |
|---|---|---|
| 小程序端（含开发者工具模拟器） | `WX_API_TOKEN` | ✅ |
| 微信定时触发器 | `WX_TRIGGER_API_TOKEN_V0` | ✅ |
| **云开发控制台「云端测试」** | 两个都没有 | ❌ **你刚才踩的就是这个** |
| CLI 命令行 | 两个都没有 | ❌ |

**好消息**：SDK 为定时触发器专门写了分支和专属令牌变量，说明自动对账在设计上是明确支持的，
方案本身成立。只是**验证方式必须换**，不能用云端测试。

以下步骤已按此重排。

---

## 步骤 6 · 重新部署 `reconcilePayment`

> ✅ **若你已用步骤 8「方式二」手工修好订单，本步可跳过** —— 步骤 9 本身就会重新部署，会一并把最新代码推上云端，这里再做一次是重复劳动。

你在步骤 3 部署之后，我又提交了 3 次改动（资金校验、异常落库、令牌自检），
这些**只在你本地磁盘上，云端没有**。

微信开发者工具 → 右键 `cloudfunctions/reconcilePayment` → **上传并部署：云端安装依赖**

- [ ] 部署完成
- [ ] **回步骤 4 复查超时是否还是 60 秒**（顺便确认"重新部署会不会重置超时"这个待验证项）

---

## 步骤 7 · 自检确认认知（1 分钟，可跳过）

> ✅ **若你在原步骤 6 已实测撞到过 `invalid wx openapi access_token`，本步可跳过** —— 结论已由实测成立，不必再印证一次。

云开发控制台 → 云函数 → `reconcilePayment` → 云端测试 → 参数：

```json
{"diagnose": true}
```

**预期返回**：

```json
{
  "code": 200,
  "message": "当前调用来源拿不到微信身份令牌，cloudPay 必然失败（控制台测试/CLI 均如此，属预期）",
  "data": {
    "triggerSrc": null,
    "hasApiToken": false,
    "hasTimerToken": false,
    "cloudCallUsable": false
  }
}
```

看到 `cloudCallUsable: false` 就对了 —— 这印证了上面的解释，控制台确实没令牌。
这一步只读，不碰任何订单。

- [ ] 返回 `cloudCallUsable: false`（符合预期）

> 如果意外返回 `true`，说明我的判断有误，**停下来告诉我**。

---

## 步骤 8 · 修复事故订单

### 方式一 · 开发者工具模拟器（可选，顺带验证对账链路）

模拟器调用带 `WX_API_TOKEN`，云调用可用。

1. 微信开发者工具打开本项目，**编译运行**（让小程序真正跑起来）
2. 切到开发者工具下方的 **Console** 面板
3. 粘贴执行：

```js
wx.cloud.callFunction({
  name: 'reconcilePayment',
  data: { orderNo: 'PROD20260728072033017383' }
}).then(res => console.log(JSON.stringify(res.result, null, 2)))
  .catch(err => console.error(err))
```

**预期**：`result: "repaired"`，`outTradeNo: "PROD178522326265673"`

| 返回 | 含义 | 下一步 |
|---|---|---|
| **`repaired`** | ✅ 订单已修复，核销码已生成 | 记下核销码，通知游客，继续步骤 9 |
| `not_paid` + `states` 里还是 `QUERY_ERROR` | 模拟器这条路也没令牌 | 改走方式二 |
| `not_paid` + `AMOUNT_MISMATCH` / `CLAIMED_BY_OTHER` | 资金校验没通过 | **停，把返回发我** |
| 其他 | — | **停，把返回发我** |

- [ ] 拿到核销码

拿到后回 `orders` 集合确认：`status` = `paid`、`verificationCode` 有值、
`payment.transactionId` 和 `paidAt` 都是真实值（不是 null）。

### 方式二 · 用「高级操作」一条语句补全（数据与方式一等价）

游客已经等了两天，不想折腾方式一就直接走这个。

**先导出备份该记录**，然后在「高级操作」执行：

```js
db.collection('orders')
  .where({
    orderNo: 'PROD20260728072033017383'
  })
  .update({
    data: {
      status: 'paid',
      verificationCode: '7K3M9Q',
      'payment.outTradeNo': 'PROD178522326265673',
      'payment.transactionId': '4500000287202607283553778448',
      'payment.paidAmount': 49.9,
      'payment.paidAt': new Date('2026-07-28T15:21:09+08:00'),
      'payment.repairedBy': 'manual-2026-07-30',
      updatedAt: new Date()
    }
  })
```

- [x] 执行返回 `updated: 1`
- [x] 核销码 `7K3M9Q` 已告知游客

**每个值的来源**：

| 字段 | 值 | 依据 |
|---|---|---|
| `status` | `paid` | 与 `wechatPayCallback` 正常流程写入的值一致 |
| `verificationCode` | `7K3M9Q` | 按 `generateVerificationCode()` 的规则手搓：6 位，字符集 `0123456789ABCDEFGHJKLMNPQRSTUVWXYZ`（无 I/O） |
| `payment.outTradeNo` | `PROD178522326265673` | 微信账单「商户单号」 |
| `payment.transactionId` | `4500000287202607283553778448` | 微信账单「交易单号」 |
| `payment.paidAmount` | `49.9` | 微信账单金额，与 `order.totalAmount` 一致 |
| `payment.paidAt` | `2026-07-28 15:21:09 +08:00` | 微信账单「支付时间」 |
| `payment.repairedBy` | `manual-2026-07-30` | 人工补单留痕，便于日后对账时识别 |

> **这些字段与对账函数会写入的完全等价**。微信查单返回的 `time_end` 格式是
> `YYYYMMDDHHmmss`，本身只有秒级精度，所以手工填的支付时间和函数写入的一模一样。
>
> 方式一唯一的额外收益是**顺便验证了对账链路可用**——那是流程收益，不是数据收益。
> 但步骤 10 会用定时器做同样的验证，所以走方式二并不会漏掉这个验证。

> ⚠️ 若 `new Date(...)` 在「高级操作」里报错，把那一行删掉先跑通其余字段，
> 然后告诉我，我给你换个写法。

---

## 步骤 8.5 · 先沟通，再继续部署

订单已修好，**先把人的事了了**再做后面的部署。

### 一、告知游客前必须做的两个确认

**① 核销码查重**（`scanCode` 只按码查、不带状态过滤、取第一条，而 6 位码生成时不查重）

```js
db.collection('orders')
  .where({
    verificationCode: '7K3M9Q'
  })
  .get()
```

- [ ] **只返回 1 条**（多于 1 条先别通知游客，换个码）

**② 确认订单状态**

```js
db.collection('orders')
  .where({
    orderNo: 'PROD20260728072033017383'
  })
  .get()
```

- [ ] `status` = `paid`、`verificationCode` = `7K3M9Q`、`payment.paidAt` 有值

### 二、游客会看到什么（提前知道，避免二次投诉）

刷新订单页后他会看到：状态「待核销」、核销码 `7K3M9Q`、「核销码有效期剩余 13 天」。

⚠️ **但「下单时间」和「核销截止」仍显示 `NaN-aN-aN aN:aN:aN`** —— 那是另一个前端 bug
（见 `工作清单-2026-07-29.md` 第 22 项），要等小程序发版才修。

**客服话术里主动带一句**，别等游客又来问：

> 页面上「下单时间」和「核销截止」显示异常是我们已知的显示问题，正在修复中，
> **不影响核销码使用**，有效期到 8 月 12 日。

### 三、通知顺序

- [ ] **通知游客**（话术见第三节 3.1，核销码填 `7K3M9Q`）—— 立刻，他已等两天
- [ ] **通知景区**（话术见第三节 3.2）—— 今天内

> 给景区的「影响范围」现在**还填不了具体笔数**，全量排查要等步骤 9、10 的定时器跑起来。
> 建议写：**「目前确认 1 笔，已修复完毕。全量排查机制今日部署，完成后主动同步结果。」**
>
> **不要写"仅此一笔"** —— 等步骤 10 出结果后再补一条同步，主动给结论比被追问强。

---

## 步骤 9 · 恢复定时器并部署

把 `cloudfunctions/reconcilePayment/config.json` 内容改回：

```json
{
  "triggers": [
    {
      "name": "reconcilePayment",
      "type": "timer",
      "config": "0 */10 * * * * *"
    }
  ]
}
```

重新「上传并部署：云端安装依赖」。

- [x] 部署完成
- [ ] **再次复查超时还是 60 秒**

---

## 步骤 10 · 验证定时器真的拿到了令牌（关键）

> ⚠️ **本步已修订**。原判据是「`pay_exceptions` 为空即成功」——**那是错的**。
> 扫描条件是「状态待支付 且 在时间窗口内」，若窗口内一笔待支付订单都没有，
> 函数直接返回 `scanned: 0`，**根本不会调用微信查单，自然什么都不写**。
> 此时「空」同时兼容三种情况，无法区分：
> ① 定时器正常但无单可扫 ② 定时器压根没触发 ③ 跑了但没验证到令牌。
>
> 已改为下面的心跳判据。

### 10.1 先重新部署（心跳代码是新加的）

微信开发者工具 → 右键 `cloudfunctions/reconcilePayment` → **上传并部署：云端安装依赖**

- [ ] 部署完成
- [ ] 复查超时仍是 60 秒

### 10.2 等 15 分钟，看心跳记录

定时器每次自动运行都会写一条**固定 `_id` 的心跳记录**（覆盖写，集合里永远只有这一条，不会堆积）。

数据库 → `pay_exceptions` → 应该能看到一条 `_id` 为 `_heartbeat_reconcile` 的记录：

```json
{
  "_id": "_heartbeat_reconcile",
  "type": "heartbeat",
  "triggerSrc": "timer",          ← 关键
  "hasTimerToken": true,          ← 关键
  "cloudCallUsable": true,        ← 关键
  "scanned": 0,
  "repaired": 0,
  "queriedWechat": false,
  "lastRunAt": "..."
}
```

### 结果怎么判

| 现象 | 含义 | 下一步 |
|---|---|---|
| **有心跳，`cloudCallUsable: true`** | ✅ 定时器在跑且拿到了令牌，兜底网成立 | 继续步骤 11 |
| **有心跳，`cloudCallUsable: false`** | 🔴 定时器跑了但没令牌，方案不成立 | **停，把心跳记录发我**，需换实现 |
| **完全没有心跳记录** | 🔴 定时器根本没触发 | **停，告诉我**，要查触发器配置 |

- [ ] 心跳存在且 `cloudCallUsable: true`

> **补充说明**：`queriedWechat: false` 表示本轮没有待支付订单可查，属正常。
> 此时 `cloudCallUsable` 是**根据环境变量推断**的（令牌变量确实存在），
> 尚未经过一次真实的微信调用验证。
> 真正的端到端验证会在步骤 13 的 0.01 元真实支付里完成。

### 10.3 顺带确认定时器触发频率（可选）

云开发控制台 → 云函数 → `reconcilePayment` → 监控 / 调用记录

部署后每 10 分钟应有一次调用。看一眼调用次数对不对得上，能进一步排除触发器异常。

---

## 步骤 11 · 部署 `wechatPayCallback`

- [ ] 上传并部署

**改了什么**：支付通知现在会**同时按历史单号反查订单**，7-28 那种"单号被覆盖"的情况
当场就能匹配上，不用等定时器的 10 分钟。另外补了异常留痕和并发保护。

---

## 步骤 12 · 部署 `wechatPay`

- [ ] 上传并部署

**改了什么**：同一订单**复用**已申请的商户单号，不再每次覆盖。
若微信已关掉旧单号，自动换新单号重试。

---

## 步骤 13 · 用 0.01 元真实支付验证

在体验版或正式版下一笔真实订单（把某个商品价格临时改成 0.01 元），完整付款。

- [ ] 支付后订单正常变为 `paid`（商品若无需核销则为 `completed`）
- [ ] 核销码正常生成
- [ ] `pay_exceptions` 集合里**没有**新增记录（正常流程不该留痕）

**这一步在确认什么**：步骤 11、12 改的是支付主链路，必须确认没有把现在正常工作的流程改坏。

⚠️ 任何一项不符合，**立即停止，把情况发我**，不要提交小程序审核。

---

## 步骤 14 · 提交小程序审核

前端的防重复点击、加载提示、已支付提示需要发版才生效。

- [ ] 提交审核

**提审前检查**：`app_settings` 的 `rechargeEnabled` 必须是 `false`（这是你们的既有规矩）。

**审核期间不用担心**：云端兜底网从步骤 9 起就在工作了。

---

# 三、给游客和客户的话术

## 3.1 给游客

> 您好，我是翠屏湖水上乐园的客服。
>
> 非常抱歉，您 7 月 28 日购买的双人票因系统故障未能正常出票。
>
> **您的付款是成功的，49.9 元没有丢失。**
>
> 核销码已为您重新生成：**（填步骤 6 拿到的核销码）**，
> 请在小程序「我的订单」中查看，凭此码到水上城堡入口核销即可。
>
> 作为补偿，我们为您准备了 ______，下次到店可凭订单号领取。
>
> 再次致歉，如有任何问题请随时联系我。

**要点**：先说钱没丢（游客最焦虑的就是这个），再给解决方案，最后给补偿。

## 3.2 给景区（客户）

> 关于 7 月 28 日一笔水上城堡门票订单状态异常的情况说明：
>
> **现象**：游客付款成功，微信已扣款，但小程序订单未更新为已支付，核销码未生成。
>
> **原因已完整定位**：用户在支付页绑定手机号后，系统会自动继续支付流程，
> 但该过程中页面未显示任何加载提示，用户以为操作无响应而再次点击了支付按钮。
> 两条路径并发，各自向微信申请了一个商户订单号，后一个覆盖了前一个；
> 用户实际在前一个支付窗口完成付款，通知返回时订单号已对不上，导致状态未更新。
>
> **完全是我方设计缺陷**：自动流程缺少提示、按钮缺少重复提交保护、
> 商户订单号被覆盖而非复用——三个问题叠加所致，与用户操作无关。
>
> **影响范围**：目前确认 1 笔，已修复完毕。全量排查机制今日部署，完成后我们会主动同步排查结果。
>
> **修复措施**：
> 1. 加载过程显示提示，杜绝"看起来没反应"
> 2. 支付入口增加提交锁，无法重复发起
> 3. 同一订单复用商户订单号，不再覆盖
> 4. 新增自动对账，每 10 分钟主动向微信核对，异常订单自动修复
> 5. 增加异常留痕，同类问题今后可完整追溯
>
> 第 4、5 项已于今日上线生效，第 1~3 项随下一版小程序发布。

**三个不要**：
- ❌ 不要承诺"以后绝不会再发生"，改说"异常最长 10 分钟自动修复"，可交付可验证
- ❌ 不要说"就这一笔"，除非步骤 7 已经确认
- ❌ 不要主动提其他安全问题，那是另一条线，一次说太多会失控

---

# 四、上线后的日常观察

定期翻 `pay_exceptions` 集合，按 `type` 字段判断：

| `type` | 含义 | 该怎么做 |
|---|---|---|
| `heartbeat`（`_id` 为 `_heartbeat_reconcile`） | 定时对账心跳，**不是异常**，固定一条覆盖写 | 看 `cloudCallUsable` 和 `lastRunAt` 判断定时器是否健康 |
| `cloudpay_token_unavailable` | **定时器拿不到云调用令牌，兜底网失效** | 🔴 **方案要换实现**，见步骤 10 |
| `amount_mismatch` | 微信返回金额与订单金额不符 | 🔴 **人工核实**，可能是串单 |
| `trade_no_claimed` | 该商户单号已归属其他订单 | 🔴 **人工核实**，可能有人试图顶账 |
| `callback_on_abnormal_status` | **钱已收但订单已取消或退款** | 🔴 **人工介入**，核实退款或补发 |
| `query_order_error` | 向微信查单报了其他错 | 看 `errorMessage`，偶发可忽略，持续出现要查 |
| `order_not_found` | 商户单号对不上 | 小程序新版发布后应归零；若仍出现说明还有未知的重复下单路径 |
| `order_repaired` | 对账自动补单成功 | 正常，但要看频次，频繁说明回调链路有问题 |
| `callback_duplicate` | 订单已被对账处理过 | 正常，说明兜底网在工作 |
| `callback_not_success` | 回调返回非成功状态 | 看 `rawEvent` 字段排查 |

---

# 五、本次没解决的问题

根因已修，但以下几项经审查确认存在、本批未处理：

- 同一订单被**扣款两次**时只处理第一笔（需人工介入，非自动止损）
- 对账和回调两条路径均**无金额校验**
- `orders` 集合缺 `status` + `createdAt` 组合索引，数据量大后扫描会慢
- 无待支付订单自动关单机制，废单会一直堆在库里

**更严重的问题在别处**——鉴权漏洞、微信支付的游船订单押金可能从未退还等，
见 `工作清单-2026-07-29.md`，那些和本次事故是独立的两条线。

---

# 附录 · 修订记录

本手册在执行过程中修订过，记录如下，便于回溯。

| 时间 | 修订内容 | 原因 |
|---|---|---|
| 初版 | 超时配置放在「部署前必做」 | ❌ 错误：函数未部署时控制台无该配置项，已移到步骤 4 |
| 修订一 | 步骤 5 用「添加字段」弹窗加 `payment.outTradeNoHistory` | ❌ 风险：点号命名可能生成字面顶层字段。改用「高级操作」执行 update 语句 |
| 修订一 | 声称"控制台没有语句执行入口" | ❌ 错误：数据库页左下角「高级操作」就是（语法不是 SQL，是云开发 JS 语法） |
| 修订二 | 步骤 6、7 用「云端测试」跑对账 | ❌ **根本性错误**：云调用需微信侧令牌，控制台测试永远拿不到。已改为模拟器调用 + 定时器验证 |

**应急方案已并入步骤 8 的「方式二」**，不再单列。

---

# 附录二 · 关键事实速查

| 事实 | 依据 |
|---|---|
| 云调用令牌只在小程序端调用和微信定时触发器时存在 | `@cloudbase/node-sdk/lib/utils/wxCloudToken.js` 源码注释 |
| 云函数默认超时 3 秒，上限 60 秒，`config.json` 无 timeout 字段 | 微信官方定时触发器文档 |
| 云开发写入不存在的集合会报 `-502005`，不会自动建集合 | 本项目 `admin_logs` 历史踩坑 |
| SDK 会把入参驼峰自动转下划线、返回值下划线转驼峰 | `wx-server-sdk` 源码 `convertCase` 调用 |
| 微信允许对未支付订单用同一 `out_trade_no` 重入下单；已支付返回 `ORDERPAID`；已关单需换新单号 | 微信支付 V2 官方文档 |
| 事故订单真实扣款单号 `PROD178522326265673`，交易单号 `4500000287202607283553778448` | 游客提供的微信账单截图 |
